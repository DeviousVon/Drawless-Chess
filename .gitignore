node_modules/
build/
.pnpm-store/
.agents/
pids/
*.class

# Local editor, OS, profiling, service, and signing material.
.idea/
.vscode/
*.iml
.DS_Store
Thumbs.db
**/xcuserdata/
*.xcuserstate
.env
.env.*
.claude/
key.properties
android/signing.properties
google-services.json
*.jks
*.keystore
*.key
*.der
*.pk8
*.pem
*.p12
*.pfx
*.hprof
*.trace
captures/
TEMP/

# Recreated from the pinned upstream revision and ordered Drawless patches.
# Exact prepared source is included in each corresponding-source release archive.
engine/native/upstream/Fairy-Stockfish/

# Obsolete mirror copies retained locally for now; authoritative files live at root.
src/docs/
src/engine/
src/scripts/

# Android/Gradle machine-local state and generated binaries
android/.gradle/
android/.kotlin/
android/local.properties
android/**/build/
android/**/.cxx/
multiplatform/.gradle/
multiplatform/.kotlin/

# Local verification evidence and generated release archives. The deterministic
# dependency inventory and SBOM under release/reports remain versioned.
release/machine/
release/reports/*-instrumentation.txt
release/*.tar.gz
play/store-assets/source-captures/

# Separate local tooling checkout; it has its own repository and is not part of Drawless Chess.
/Audacity-MCP/
